export type BranchPointSelection = {
  branchId: string
  pointIndex: number
} | null

export type OrbitPointSelection = {
  orbitId: string
  pointIndex: number
} | null

export type LimitCyclePointSelection = {
  limitCycleId: string
  pointIndex: number
} | null
